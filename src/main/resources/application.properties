spring.application.name=cic

spring.profiles.active=prod

# Local PostgreSQL Docker container
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://postgres:5432/cic}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:123Thineth//}
spring.datasource.driver-class-name=org.postgresql.Driver
spring.jpa.hibernate.ddl-auto=update

#spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:postgresql://db.qjxxgurmxbbncjebrpgq.supabase.co:5432/}
#spring.datasource.username=${SPRING_DATASOURCE_USERNAME:postgres}
#spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:123Thineth//}


# File upload
spring.servlet.multipart.max-file-size=20MB
spring.servlet.multipart.max-request-size=20MB

# Google Cloud Storage
gcs.bucket-name=my-intranet-files
gcs.credentials-path=${CREDENTIAL_PATH:classpath:gcs-credentials.json}

# JPA/Hibernate
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect

spring.task.scheduling.enabled=true

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://192.168.8.50,https://cicintranet.netlify.app
cors.allowed-origins=${CORS_ALLOWED_ORIGINS:http://localhost:5173}
#bb

# Auth
auth.validator.type=stem-link
jwt.secret=${JWT_KEY}
audit.log.retention-hours=96
app.jwt.secret=${jwt.secret}
app.jwt.expiration-ms=28800000

# Email
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=${EMAIL:info.cicfeedsgrp@gmail.com}
spring.mail.password=${PASSWORD:zhmbjpffmbiuaixa}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

# Ticket URL
app.ticket.url=http://192.168.8.50/admin/ticket

# superuser
app.superuser.email=${SEND_EMAIL:thinethwic@gmail.com}

# Force Jackson to use field access, not getter/setter
spring.jackson.mapper.use-annotations=true